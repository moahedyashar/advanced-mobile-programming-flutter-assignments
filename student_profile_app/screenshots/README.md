# Screenshots

Included files:

- `profile.png` — submission preview showing the Student Profile with availability enabled.
- `preview.html` — reproducible source used to render the preview while the local Flutter runner was unavailable.

For a final device-specific capture, run the app in an emulator and replace `profile.png`.
