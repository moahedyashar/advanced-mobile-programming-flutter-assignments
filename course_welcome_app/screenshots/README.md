# Screenshots

Included files:

- `home.png` — submission preview showing the Course Welcome screen after one interaction (`1/3`).
- `preview.html` — reproducible source used to render the preview while the local Flutter runner was unavailable.

For a final device-specific capture, run the app in an emulator and replace `home.png`.
